{
  "name": "whisk-a-way",
  "version": "1.0.0",
  "private": true,
  "description": "Whisk-a-Way full-stack application workspace",
  "scripts": {
    "start": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run start --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
